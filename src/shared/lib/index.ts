export { supabase, getNotes, createNote, deleteNote, updateNote } from './supabase/client';
export { signUp, signIn, signOut, getCurrentUser, onAuthStateChange, resetPassword, updatePassword, getProfile, updateProfile } from './supabase/auth';
export { cn } from './utils';
